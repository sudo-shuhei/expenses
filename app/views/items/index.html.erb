<h2>支出履歴</h2>
<% @items.each do |item| %>
  <div class ="p-2 m-1 border text-dark">
    <%= link_to "/items/#{item.id}", class: "text-dark" do %>
      <%= item.memo %>
      <br>
      ¥<%= item.price %>
      <br>
    <% end %>
  </div>
<% end %>
<br>
